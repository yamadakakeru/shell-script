#ymlファイル

name: clone_test

on:
  workflow_dispatch:

jobs:
    build:
        name: clone
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v2
            - name: clone sh
              run: sh ./clone.sh   
